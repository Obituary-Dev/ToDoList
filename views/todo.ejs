<!DOCTYPE html>

<html>
    <head>
        <title>ToDo List</title>
        <style>
            .sup {text-decoration: none; color: red;}
            .mod {text-decoration: none; color: blue;}
        </style>
    </head>

    <body>
        <h1>To do list</h1>

        <ul>
        <% todolist.forEach(function(todo, index) { %>
            <li><a class="sup" href="/todo/delete/<%= index %>">✘</a> <a class="mod" href="/todo/see/<%= index %>">►</a> <%= todo.description %> (<%= todo.priority %>)</li>
        <% }); %>
        </ul>

        <form action="/todo/add/" method="post">
            <p>
                <label for="newtodo">What do I still have to do?</label>
                <input type="text" name="newtodo" id="newtodo" autofocus />
                <input type="text" name="priority" id="priority" maxlength="1" size="1"/>
                <input type="submit" />
            </p>
        </form>
    </body>
</html>